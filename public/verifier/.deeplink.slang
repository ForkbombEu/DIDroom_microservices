Prepare 'res': connect to 'verifier_endpoint' and do post where object is 'object'

Given I have a 'string' in path 'res.result.intent_url'
Then print the 'intent_url'

Compute: store in file where path is 'deeplink_path', content is 'intent_url'

