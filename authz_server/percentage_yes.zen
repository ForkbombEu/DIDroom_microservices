Given I have a 'integer' named 'percentage'


When I set 'modulo' to '100' as 'integer'
and I create the array of '1' random numbers modulo 'modulo'
and I create the copy of element '1' from array 'array'

If I verify number 'copy' is less than 'percentage'
When I write the string 'yes' in 'result'
EndIf

If I verify number 'copy' is more or equal than 'percentage'
When I write the string 'no' in 'result'
EndIf

Then print the 'result'
