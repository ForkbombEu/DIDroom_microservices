on:
  push:
    branches:
      - 'main'
  pull_request:

jobs:
  api_test:
    runs-on: ubuntu-latest
    name: 🧪 VCI API Tests
    steps:
      - uses: actions/checkout@v3
      - run: make &
      - run: npx stepci run "tests/credential_issuer.yml"
