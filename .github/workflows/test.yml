on: [push, pull_request]
jobs:
  api_test:
    runs-on: ubuntu-latest
    name: 🧪 VCI API Tests
    steps:
      - run: hostname
      - uses: actions/checkout@v3
      - run: make &
      - uses: stepci/stepci@main
        with:
          workflow: "tests/credential_issuer.yml"
          envs: host=${{ $(hostname) }}:3000 
