on: [push, pull_request]
jobs:
  api_test:
    runs-on: ubuntu-latest
    name: 🧪 VCI API Tests
    steps:
      - run: |
          echo "HOST=$(hostname)" >> $GITHUB_ENV
      - uses: actions/checkout@v3
      - run: make &
      - uses: stepci/stepci@main
        with:
          workflow: "tests/credential_issuer.yml"
          envs: host="${{ env.HOST }}:3000" 
